import Header from "./Header";

const ScoringSystem = ({ laughMeter, techScore, uiScore }) => (
  <Header laughMeter={laughMeter} techScore={techScore} uiScore={uiScore} />
);

export default ScoringSystem;
